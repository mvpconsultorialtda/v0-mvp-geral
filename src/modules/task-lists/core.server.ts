
export const getTaskLists = () => ([]);
export const getTaskListById = (id: string) => ({});
export const createList = async (name: string, ownerId: string) => ({ id: 'new-list-id', name, ownerId });
export const createTask = (listId: string, text: string) => ({});
